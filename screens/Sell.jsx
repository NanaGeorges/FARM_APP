import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const Sell = () => {
  return (
    <SafeAreaView>
      <Text> Add a product or book a service </Text>
    </SafeAreaView>
  )
}

export default Sell

const styles = StyleSheet.create({})